// State lives in WatchedContext so all consumers share the same instance.
// This re-export keeps all existing import paths working unchanged.
export { useWatched } from '../context/WatchedContext';
